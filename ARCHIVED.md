# ⚠️ ARCHIVED — concept prototype, not the product

This root app (`09_HebrewBites`, Expo SDK 52, hardcoded glass UI) is an **archived
design/concept prototype**. It is **no longer developed** — do not add features here.

**The active product is `TAL/mobile-app`** (repo `talmannn/hebrebites-mobile-app`,
Expo SDK 56, real backend + study flow). All mobile work happens there.

The 4-sided learn-flow design prototype that briefly lived here was reverted
(see commit history around `c92c264` / `5f57a66`) and is recoverable from git if
needed as a visual reference.
